server:
  port: 80
  tomcat:
    uri-encoding: UTF-8
spring:
  datasource:
    url: jdbc:mysql://192.168.1.240:3306/testdb?characterEncoding=utf8&autoReconnect=true&useSSL=false
    username: root
    password: admin
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect